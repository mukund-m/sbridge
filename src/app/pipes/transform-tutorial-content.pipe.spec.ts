import { TransformTutorialContentPipe } from './transform-tutorial-content.pipe';

describe('TransformTutorialContentPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformTutorialContentPipe();
    expect(pipe).toBeTruthy();
  });
});
